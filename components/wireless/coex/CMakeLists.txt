sdk_generate_library()

sdk_add_include_directories(./)

# if("${CHIP}" STREQUAL "bl616")
#    sdk_add_private_include_directories(./reg/bl616)
# endif()

if("${CHIP}" STREQUAL "bl616")
    sdk_add_private_compile_definitions(-DBL616)
endif()

if(CONFIG_COEX_BT_MODE)
sdk_add_private_compile_definitions(-DCONFIG_COEX_BT_MODE=${CONFIG_COEX_BT_MODE})
else()
sdk_add_private_compile_definitions(-DCONFIG_COEX_BT_MODE=0)
endif()
if(CONFIG_COEX_WIFI_MODE)
sdk_add_private_compile_definitions(-DCONFIG_COEX_WIFI_MODE=${CONFIG_COEX_WIFI_MODE})
else()
sdk_add_private_compile_definitions(-DCONFIG_COEX_WIFI_MODE=0)
endif()
if(CONFIG_COEX_THREAD_MODE)
sdk_add_private_compile_definitions(-DCONFIG_COEX_THREAD_MODE=${CONFIG_COEX_THREAD_MODE})
else()
sdk_add_private_compile_definitions(-DCONFIG_COEX_THREAD_MODE=0)
endif()
if(CONFIG_COEX_TDMA_NONE)
sdk_add_private_compile_definitions(-DCONFIG_COEX_TDMA_NONE=${CONFIG_COEX_TDMA_NONE})
else()
sdk_add_private_compile_definitions(-DCONFIG_COEX_TDMA_NONE=0)
endif()

sdk_library_add_sources(coex.c coex_tdma.c coex_pti.c)
