sdk_add_subdirectory_ifdef(CONFIG_BLUETOOTH bluetooth)
sdk_add_subdirectory_ifdef(CONFIG_WL80211 wl80211)
if(CONFIG_WIFI6)
    sdk_add_include_directories(macsw/inc)
    sdk_add_compile_definitions(-DCONFIG_MACSW_SELECT_INCLUDE=\"macsw_${CONFIG_MACSW_SELECT}_config.h\")
    if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/macsw/lib/libmacsw_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    sdk_add_static_library(macsw/lib/libmacsw_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    else()
    sdk_add_static_library(${SDK_DEMO_PATH}/${BUILD_DIR}/build_macsw/libmacsw_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    endif()
    if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/macsw/lib/libmacsw_config_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    sdk_add_static_library(macsw/lib/libmacsw_config_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    else()
    sdk_add_static_library(${SDK_DEMO_PATH}/${BUILD_DIR}/build_macsw/libmacsw_config_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    endif()
endif()

sdk_add_compile_definitions_ifdef(CONFIG_TIMESLICE_STATS_DEBUG -DMACSW_TIMESLICE_STATS_DEBUG=1)

if(CONFIG_FHOST)
    sdk_add_include_directories(wifi6/fhost/include)
    if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/wifi6/fhost/lib/libfhost_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    sdk_add_static_library(wifi6/fhost/lib/libfhost_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    else()
    sdk_add_static_library(${SDK_DEMO_PATH}/${BUILD_DIR}/build_fhost/libfhost_${CHIP}_${CONFIG_MACSW_SELECT}.a)
    endif()
    sdk_add_subdirectory_ifexists(wifi6)
endif()
sdk_add_subdirectory_ifdef(CONFIG_BL_WPA_SUPPLICANT bl_wpa_supplicant)
sdk_add_subdirectory_ifdef(CONFIG_LPAPP lpfw_private)
if(CONFIG_LMAC154)
    sdk_add_include_directories(lmac154/lmac154/include)
    if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/lmac154/lmac154/lib/liblmac154_${CHIP}.a)
      sdk_add_static_library(lmac154/lmac154/lib/liblmac154_${CHIP}.a)
    else()
      sdk_add_include_directories(lmac154/lmac154/src)
      sdk_add_static_library(${SDK_DEMO_PATH}/${BUILD_DIR}/build_lmac154/liblmac154_${CHIP}.a)
    endif()
endif()

if(CONFIG_THREAD)
  sdk_add_subdirectory_ifexists(thread)

  if(CONFIG_OTBR)
    sdk_add_include_directories(thread/openthread_br/include)
    if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/thread/openthread_br/lib/libopenthread_br_${CHIP}.a)
      sdk_add_static_library(thread/openthread_br/lib/libopenthread_br_${CHIP}.a)
    else()
      sdk_add_static_library(${SDK_DEMO_PATH}/${BUILD_DIR}/build_openthread_br/libopenthread_br_${CHIP}.a)
    endif()
  endif()
endif()

sdk_add_subdirectory_ifdef(CONFIG_COEX coex)
sdk_add_subdirectory_ifdef(CONFIG_MATTER matter/mfd)
sdk_add_subdirectory_ifdef(CONFIG_SUBG bl3141_drv)
sdk_add_subdirectory_ifdef(CONFIG_ZIGBEE zigbee)
sdk_add_subdirectory_ifdef(CONFIG_WLAN wlan/wlan_mac)
sdk_add_subdirectory_ifdef(CONFIG_WIFI4 wifi4)

if(CONFIG_WIFI6)
sdk_generate_library(macsw_plat)
sdk_library_add_sources(macsw_plat.c)
endif()
