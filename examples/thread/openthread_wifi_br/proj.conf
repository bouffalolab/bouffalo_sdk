if(EXISTS .config.cmake)
include(.config.cmake)
endif()

# Components
set(CONFIG_NEWLIB                n)
set(CONFIG_NEWLIB_STANDARD       n)
set(CONFIG_BFLB_LOG                 n)
set(CONFIG_FREERTOS              y)
set(CONFIG_POSIX                 n)
set(CONFIG_TLSF                  y)
set(CONFIG_SHELL                 y)
set(CONFIG_LWIP                  y)
set(CONFIG_FHOST                 y)
set(CONFIG_WIFI6                 y)
set(CONFIG_RF                    y)
set(CONFIG_DHCPD                 y)
set(CONFIG_PING                  y)

set(CONFIG_BFLB_MTD              y)
set(CONFIG_PARTITION             y)
set(CONFIG_LITTLEFS              y)

set(CONFIG_VSNPRINTF_NANO        y)
set(CONFIG_VSNPRINTF_FLOAT       n)
set(CONFIG_VSNPRINTF_FLOAT_EX    n)
set(CONFIG_VSNPRINTF_LONG_LONG   y)
set(CONFIG_COREDUMP              y)

# mbedtls
set(CONFIG_MBEDTLS               y)
set(CONFIG_MBEDTLS_AES_USE_HW    y)
set(CONFIG_MBEDTLS_BIGNUM_USE_HW y)
set(CONFIG_MBEDTLS_ECC_USE_HW    y)
set(CONFIG_MBEDTLS_SHA1_USE_HW   y)
set(CONFIG_MBEDTLS_SHA256_USE_HW y)
set(CONFIG_MBEDTLS_SHA512_USE_HW y)

# thread
set(CONFIG_LMAC154               y)
set(CONFIG_THREAD                y)

# coex
set(CONFIG_COEX                  y)
set(CONFIG_COEX_WIFI_MODE        1)
set(CONFIG_COEX_THREAD_MODE      1)
set(CONFIG_COEX_TDMA_NONE        2)

set(CONFIG_OTBR                  y)
set(CONFIG_IPV6                  y)
