if(IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/src)
    sdk_generate_library()
    sdk_add_private_include_directories(include)
    if("${CHIP}" STREQUAL "bl616cl")
        sdk_library_add_sources(src/bflb_mjdec_v2.c)
    endif()
    if("${CHIP}" STREQUAL "bl618dg")
        sdk_library_add_sources(src/bflb_dma2d.c)
        sdk_library_add_sources(src/bflb_dsi.c)
        sdk_library_add_sources(src/bflb_dvp_raster.c)
        sdk_library_add_sources(src/bflb_mjdec.c)
        sdk_library_add_sources(src/bflb_osd.c)
    endif()
else ()
    if((EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/lib/libmultimedia_${CHIP}_b0.a) AND ("${CPU_MODEL}" STREQUAL "b0"))
        sdk_add_static_library(lib/libmultimedia_${CHIP}_b0.a)
    elseif(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/lib/libmultimedia_${CHIP}.a)
        sdk_add_static_library(lib/libmultimedia_${CHIP}.a)
    else()
        message(FATAL_ERROR "no libmultimedia_${CHIP}.a found, please build it first")
    endif()
endif()
