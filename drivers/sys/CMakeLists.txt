sdk_generate_library()

sdk_add_include_directories(.)

sdk_library_add_sources(${CHIP}/${CHIP}_sys.c)

string(TOLOWER "${CHIP}" CHIP_LOWER)
if(CHIP_LOWER MATCHES "^(bl616|bl616cl|bl618dg)$")
if ("${CHIP}" STREQUAL "bl618dg")
if ("${CPU_MODEL}" STREQUAL "a0")
  sdk_library_add_sources(${CHIP}/${CHIP}_lp.c)
  sdk_library_add_sources(${CHIP}/${CHIP}_xip_recovery.c)
  sdk_library_add_sources(${CHIP}/${CHIP}_32k_trim.c)
endif()
else()
  sdk_library_add_sources(${CHIP}/${CHIP}_lp.c)
  sdk_library_add_sources(${CHIP}/${CHIP}_xip_recovery.c)
  sdk_library_add_sources(${CHIP}/${CHIP}_32k_trim.c)
endif()
if(CONFIG_WIFI6)
  sdk_library_add_sources(${CHIP}/${CHIP}_pm_wifi.c)
endif()
endif()

if("${CHIP}" STREQUAL "bl702")
    if(CONFIG_PM)
        sdk_library_add_sources(bl702/bl702_xip_recovery.c bl702/bl702_lp_debug.c)
    endif()
endif()

sdk_add_include_directories(${CHIP})
