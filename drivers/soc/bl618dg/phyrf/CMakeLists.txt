sdk_add_include_directories(include)
if ("${CPU_MODEL}" STREQUAL "b0")
    sdk_add_static_library(lib-gcc_10.2.0-toolchain_V2.6.1/libbl618dg_phyrf_b0.a)
else()
    if(CONFIG_PHY_CLI)
    sdk_add_static_library(lib-gcc_10.2.0-toolchain_V2.6.1/libbl618dg_phyrf_4mfg.a)
    else()
    sdk_add_static_library(lib-gcc_10.2.0-toolchain_V2.6.1/libbl618dg_phyrf.a)
    endif()
endif()
